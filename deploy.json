{
  "mcpServers": {
    "TextToImage": {
      "name": "本地文生图代理（支持中文）",
      "type": "sse",
      "command": "node",
      "args": ["src/index.js"],
      "isActive": true,
      "description": "支持中文 Prompt 的本地文生图 MCP 服务，调用阿里云通义万相。"
    }
  }
}
