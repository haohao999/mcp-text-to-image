{
  "tools": [
    {
      "name": "generate_image",
      "description": "根据文本描述生成图像",
      "inputSchema": {
        "type": "object",
        "properties": {
          "prompt": {
            "type": "string",
            "description": "图像的描述，支持中文"
          }
        },
        "required": ["prompt"]
      }
    }
  ]
}
